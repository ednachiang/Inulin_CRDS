---
title: "Code_TestCorrelations"
author: "Edna Chiang"
date: "12/14/2021"
output: html_document
---

### Load Packages, scripts, and functions
```{r}
library(ggplot2)
library(plyr)
library(dplyr)
library(grid)
library(MESS)
library(pgirmess)
library(Bolstad2)
library(phyloseq)
library(psych)
library(car)
source('scripts/import_crds.R')
source('scripts/bin_crds.R')
source('scripts/misc.R')
source('scripts/calc_delta_metrics.R')
set.seed(1)
theme_set(theme_bw())

### Stats Notes
# Use 'subset' in kruskal.test
# Do NOT use 'subset' in kruskalmc or pairwise.wilcox.test; it doesn't work!
```

### Load Data
```{r}
crds <- import.crds('CSV', 'metadata.csv')
```

### Bin RelTime
```{r}
crds.bin <- bin.crds(crds)
```


### Summarize by Substrate + ABX
```{r}
### Remove Inulin outliers
#crds.rem <- crds.bin[-which(crds.bin$ID == "S34"),]
crds.rem <- crds.bin[-which(crds.bin$ID == "S37"),]
  # ABX didn't work; high [DNA]
crds.rem <- crds.rem[-which(crds.rem$ID == "P28"),]
#crds.rem <- crds.rem[-which(crds.rem$ID == "P30"),]

# Remove Saline outliers
#crds.rem <- crds.rem[-which(crds.rem$ID == "W26"),]
crds.rem <- crds.rem[-which(crds.rem$ID == "W43"),]
crds.rem <- crds.rem[-which(crds.rem$ID == "W50"),]
crds.rem <- crds.rem[-which(crds.rem$ID == "P04"),]
  # This is an adult, not pup

crds.rem$Season <- ordered(crds.rem$Season, levels=c("Summer", "Winter", "Spring"))


# Summarize
crds.sum <- crds.rem %>%
  group_by(Substrate, Antibiotics, Season, BinTime) %>%
  summarize(Y_Mean = mean(Delta),
            Y_Median = median(Delta),
            Y_SD = sd(Delta),
            Y_SE = se(Delta),
            Y_IQR = IQR(Delta),
            X_Mean = mean(RelTime),
            X_Median = median(RelTime),
            X_SD = sd(RelTime),
            X_SE = se(RelTime),
            X_IQR = IQR(RelTime),
            SampSize = n())
  # X_Mean is different from BinTime because BinTime was calculated with ABX and non-ABX combined
    # BinTime was calculated this way to make sure datapoints for all individuals will align together on x-axis. If datapoints are misaligned that can be confusing for the audience. So binning was done this way for the clearest visualization.
    # X_Mean is calculated for ABX and non-ABX separately, so there will be some variation
crds.sum$Season <- ordered(crds.sum$Season, levels=c("Summer", "Winter", "Spring"))
```


### Calculate Delta Metrics
```{r}
# Calculate delta metrics
d.metrics <- calc.delta.metrics(crds.rem)
#write.csv(d.metrics, "delta_metrics.csv", row.names = F)

# Load metadata
meta <- read.csv('metadata.csv')

# Add metadata to delta metrics
d.metrics[,colnames(meta)[3:12]] <- NA
for(sq in 1:nrow(d.metrics)){
  meta.row <- which(meta$ID == d.metrics$ID[sq])
  d.metrics[sq, 6:15] <- meta[meta.row, 3:12]
}

# Convert to numeric
d.metrics$MaxD <- as.numeric(d.metrics$MaxD)
d.metrics$TimeMaxD <- as.numeric(d.metrics$TimeMaxD)
d.metrics$Slope <- as.numeric(d.metrics$Slope)
d.metrics$SlopeTime <- as.numeric(d.metrics$SlopeTime)

# Order seasons
d.metrics$Season <- ordered(d.metrics$Season, levels=c("Summer", "Winter", "Spring"))

# Separate substrates
d.inu <- d.metrics[which(d.metrics$Substrate == "Inulin"),]
d.sal <- d.metrics[which(d.metrics$Substrate == "Saline"),]

# Separate by substrate + ABX
d.inu.abx <- d.inu[which(d.inu$Antibiotics == "Yes"),]
d.inu.noabx <- d.inu[which(d.inu$Antibiotics == "No"),]
d.sal.abx <- d.sal[which(d.sal$Antibiotics == "Yes"),]
d.sal.noabx <- d.sal[which(d.sal$Antibiotics == "No"),]
```



### Test Slope
```{r}
### Test BodyMass Correlation
cor.test(d.inu.abx$Slope, d.inu.abx$BodyMass, method = "kendall")
  # p-value = 0.332
cor.test(d.inu.noabx$Slope, d.inu.noabx$BodyMass, method = "kendall")
  # p-value = 0.02215
  # adj p = 0.0886
cor.test(d.sal.abx$Slope, d.sal.abx$BodyMass, method = "kendall")
  # p-value = 0.1124
cor.test(d.sal.noabx$Slope, d.sal.noabx$BodyMass, method = "kendall")
  # p-value = 1
p.adjust(c(0.332, 0.02215, 0.1124, 1), "BH")
  # 0.4426667 0.0886000 0.2248000 1.0000000



### Test BodyTemp Correlation
cor.test(d.inu.abx$Slope, d.inu.abx$BodyTemp, method = "kendall")
  # p-value = 0.6962
cor.test(d.inu.noabx$Slope, d.inu.noabx$BodyTemp, method = "kendall")
  # p-value = 0.5605
cor.test(d.sal.abx$Slope, d.sal.abx$BodyTemp, method = "kendall")
  # p-value = 0.7002
cor.test(d.sal.noabx$Slope, d.sal.noabx$BodyTemp, method = "kendall")
  # p-value = 0.2317



### Test Ketones Correlation
cor.test(d.inu.abx$Slope, d.inu.abx$Ketones, method = "kendall")
  # p-value = 0.112
cor.test(d.inu.noabx$Slope, d.inu.noabx$Ketones, method = "kendall")
  # p-value = 0.008291*
  # tau = -0.5002975
  # adj p = 0.033164*
cor.test(d.sal.abx$Slope, d.sal.abx$Ketones, method = "kendall")
  # p-value = 0.9702
cor.test(d.sal.noabx$Slope, d.sal.noabx$Ketones, method = "kendall")
  # p-value = 0.7203
p.adjust(c(0.112, 0.008291, 0.9702, 0.7203), "BH")
  # 0.224000 0.033164 0.970200 0.960400
```




### Test SlopeTime
```{r}
### Test BodyMass Correlation
cor.test(d.inu.abx$SlopeTime, d.inu.abx$BodyMass, method = "kendall")
  # p-value = 0.2202
cor.test(d.inu.noabx$SlopeTime, d.inu.noabx$BodyMass, method = "kendall")
  # p-value = 0.4872
cor.test(d.sal.abx$SlopeTime, d.sal.abx$BodyMass, method = "kendall")
  # p-value = 0.1199
cor.test(d.sal.noabx$SlopeTime, d.sal.noabx$BodyMass, method = "kendall")
  # p-value = 0.2086


### Test BodyTemp Correlation
cor.test(d.inu.abx$SlopeTime, d.inu.abx$BodyTemp, method = "kendall")
  # p-value = 0.8312
cor.test(d.inu.noabx$SlopeTime, d.inu.noabx$BodyTemp, method = "kendall")
  # p-value = 0.3908
cor.test(d.sal.abx$SlopeTime, d.sal.abx$BodyTemp, method = "kendall")
  # p-value = 0.3964
cor.test(d.sal.noabx$SlopeTime, d.sal.noabx$BodyTemp, method = "kendall")
  # p-value = 0.6418


### Test Ketones Correlation
cor.test(d.inu.abx$SlopeTime, d.inu.abx$Ketones, method = "kendall")
  # p-value = 0.08414
cor.test(d.inu.noabx$SlopeTime, d.inu.noabx$Ketones, method = "kendall")
  # p-value = 0.4386
cor.test(d.sal.abx$SlopeTime, d.sal.abx$Ketones, method = "kendall")
  # p-value = 0.783
cor.test(d.sal.noabx$SlopeTime, d.sal.noabx$Ketones, method = "kendall")
  # p-value = 0.7135
```




### Test MaxD
```{r}
### Test BodyMass Correlation
cor.test(d.inu.abx$MaxD, d.inu.abx$BodyMass, method = "kendall")
  # p-value = 0.73
cor.test(d.inu.noabx$MaxD, d.inu.noabx$BodyMass, method = "kendall")
  # p-value = 0.03075
  # adj P = 0.123
cor.test(d.sal.abx$MaxD, d.sal.abx$BodyMass, method = "kendall")
  # p-value = 0.1349
cor.test(d.sal.noabx$MaxD, d.sal.noabx$BodyMass, method = "kendall")
  # p-value = 0.3968
p.adjust(c(0.73, 0.03075, 0.1349, 0.3968), "BH")


### Test BodyTemp Correlation
cor.test(d.inu.abx$MaxD, d.inu.abx$BodyTemp, method = "kendall")
  # p-value = 0.7497
cor.test(d.inu.noabx$MaxD, d.inu.noabx$BodyTemp, method = "kendall")
  # p-value = 0.1681
cor.test(d.sal.abx$MaxD, d.sal.abx$BodyTemp, method = "kendall")
  # p-value = 0.7851
cor.test(d.sal.noabx$MaxD, d.sal.noabx$BodyTemp, method = "kendall")
  # p-value = 0.5934


### Test Ketones Correlation
cor.test(d.inu.abx$MaxD, d.inu.abx$Ketones, method = "kendall")
  # p-value = 0.8916
cor.test(d.inu.noabx$MaxD, d.inu.noabx$Ketones, method = "kendall")
  # p-value = 0.05591
cor.test(d.sal.abx$MaxD, d.sal.abx$Ketones, method = "kendall")
  # p-value = 0.697
cor.test(d.sal.noabx$MaxD, d.sal.noabx$Ketones, method = "kendall")
  # p-value = 0.0654
```





### Test TimeMaxD
```{r}
### Test BodyMass Correlation
cor.test(d.inu.abx$TimeMaxD, d.inu.abx$BodyMass, method = "kendall")
  # p-value = 0.861
cor.test(d.inu.noabx$TimeMaxD, d.inu.noabx$BodyMass, method = "kendall")
  # p-value = 0.06942
cor.test(d.sal.abx$TimeMaxD, d.sal.abx$BodyMass, method = "kendall")
  # p-value = 0.01527
  # adj p = 0.06108
cor.test(d.sal.noabx$TimeMaxD, d.sal.noabx$BodyMass, method = "kendall")
  # p-value = 0.6033
p.adjust(c(0.861, 0.06942, 0.01527, 0.6033), "BH")


### Test BodyTemp Correlation
cor.test(d.inu.abx$TimeMaxD, d.inu.abx$BodyTemp, method = "kendall")
  # p-value = 0.3027
cor.test(d.inu.noabx$TimeMaxD, d.inu.noabx$BodyTemp, method = "kendall")
  # p-value = 0.9023
cor.test(d.sal.abx$TimeMaxD, d.sal.abx$BodyTemp, method = "kendall")
  # p-value = 0.2639
cor.test(d.sal.noabx$TimeMaxD, d.sal.noabx$BodyTemp, method = "kendall")
  # p-value = 0.506


### Test Ketones Correlation
cor.test(d.inu.abx$TimeMaxD, d.inu.abx$Ketones, method = "kendall")
  # p-value = 0.1081
cor.test(d.inu.noabx$TimeMaxD, d.inu.noabx$Ketones, method = "kendall")
  # p-value = 0.7498
cor.test(d.sal.abx$TimeMaxD, d.sal.abx$Ketones, method = "kendall")
  # p-value = 0.03224
  # adj p = 0.12896
cor.test(d.sal.noabx$TimeMaxD, d.sal.noabx$Ketones, method = "kendall")
  # p-value = 0.8904
p.adjust(c(0.1081, 0.7498, 0.03224, 0.8904), "BH")
```






### Separate by substrate
```{r}
inu.all <- crds.sum[which(crds.sum$Substrate == "Inulin"),]
sal.all <- crds.sum[which(crds.sum$Substrate == "Saline"),]
```


### Calculate AUC
```{r}
# AUC = Area Under Curve
# Trapezoid Method

### Calculate AUC
ids <- unique(crds.rem$ID)
auc.df <- data.frame(crds.rem[1,])
auc.df <- auc.df[,-2:-3]
  # Remove Delta and CO2 cols
auc.df <- auc.df[,-13]
  # Remove BinTime col
colnames(auc.df) <- c("AUC", "Number", "ID", "Season", "Sex", "Antibiotics", "Substrate", "Diet", "Mom", "Mom_Location", "BodyMass", "BodyTemp")
auc.df$Ketones <- NA
  # Rename cols
meta <- read.csv('metadata.csv', fileEncoding="UTF-8-BOM")

#crds.rem= crds.bin
# First normalize Rel Time so 
#for (i in 2){
for (i in 1:length(ids)){
  ID <- ids[i]
  select <- crds.rem[which(crds.rem$ID == ID),]
    # Dataframe with CRDS values for 1 squirrel

  # Normalize delta value to baseline
  select$Delta <- select$Delta - select$Delta[1]
  
  # Remove baseline from AUC calculation because we care about the AUC after gavage
  # Since we already normalized delta value to baseline, we don't need that value anymore
  # Set baseline to gavage time = 0
  select[1,1] <- 0
  
  # Normalize AUC end time
  # Since length of experiment varies for each individual, we don't want experimental length to bias our AUC calculations
  # Normalize length to min time within each substrate
    # Inu = 4.0667
    # Sal = 2.21667
  if (select$Substrate == "Inulin") {
    max <- which(select$RelTime > 4.06667)
    select$RelTime[max] <- 4.06667
  } else if (select$Substrate == "Saline") {
    max <- which(select$RelTime > 2.21666667)
    select$RelTime[max] <- 2.21666667
  }

  # Create auc variable
  AUC <- 0
  
  # Run rectangle method integration - less accurate
  #for (j in 1:(nrow(select)-1)){
  #  AUC <- AUC + ((select$RelTime[j+1]-select$RelTime[j])*select$Delta[j+1])
  #}
  
  # Run trapezoid method integration - more accurate
  for(j in 1:(nrow(select)-1)){
    avgH <- (select$Delta[j+1]+select$Delta[j])/2
      # Calculates average height between start and end point
    width <- (select$RelTime[j+1]-select$RelTime[j])
    AUC <- AUC + (avgH*width)
  }
    
  auc.df[i,1] <- AUC
  auc.df[i, 2:12] <- select[1, 4:14]
  
  use <- which(meta$ID == ID)
  auc.df$Ketones[i] <- meta$Ketones[use]
}

#write.csv(auc.df, "auc.csv", row.names=F)


### Subset by Substrate & ABX
auc.inu <- auc.df[which(auc.df$Substrate == "Inulin"),]
auc.sal <- auc.df[which(auc.df$Substrate == "Saline"),]
auc.inu.s <- auc.inu[which(auc.inu$Season == "Summer"),]
auc.inu.w <- auc.inu[which(auc.inu$Season == "Winter"),]
auc.inu.p <- auc.inu[which(auc.inu$Season == "Spring"),]
auc.sal.s <- auc.sal[which(auc.sal$Season == "Summer"),]
auc.sal.w <- auc.sal[which(auc.sal$Season == "Winter"),]
auc.sal.p <- auc.sal[which(auc.sal$Season == "Spring"),]
auc.inu.abx <- auc.inu[which(auc.inu$Antibiotics == "Yes"),]
auc.inu.noabx <- auc.inu[which(auc.inu$Antibiotics == "No"),]
auc.sal.abx <- auc.sal[which(auc.sal$Antibiotics == "Yes"),]
auc.sal.noabx <- auc.sal[which(auc.sal$Antibiotics == "No"),]
```

### Test AUC
```{r}
### Test Slope Correlation ###
cor.test(auc.inu.abx$AUC, auc.inu.abx$Slope, method = "kendall")
  # p-value = 0.73
cor.test(auc.inu.noabx$AUC, auc.inu.noabx$Slope, method = "kendall")
  # p-value = 0.3536
cor.test(auc.sal.abx$AUC, auc.sal.abx$Slope, method = "kendall")
  # p-value = 0.4101
cor.test(auc.sal.noabx$AUC, auc.sal.noabx$Slope, method = "kendall")
  # p-value = 0.1126




### Test BodyMass Correlation ###
cor.test(auc.inu.abx$AUC, auc.inu.abx$BodyMass, method = "kendall")
  # p-value = 0.73
cor.test(auc.inu.noabx$AUC, auc.inu.noabx$BodyMass, method = "kendall")
  # p-value = 0.3536
cor.test(auc.sal.abx$AUC, auc.sal.abx$BodyMass, method = "kendall")
  # p-value = 0.4101
cor.test(auc.sal.noabx$AUC, auc.sal.noabx$BodyMass, method = "kendall")
  # p-value = 0.1126



### Test BodyTemp Correlation ###
cor.test(auc.inu.abx$AUC, auc.inu.abx$BodyTemp, method = "kendall")
  # p-value = 0.6445
cor.test(auc.inu.noabx$AUC, auc.inu.noabx$BodyTemp, method = "kendall")
  # p-value = 0.06168
cor.test(auc.sal.abx$AUC, auc.sal.abx$BodyTemp, method = "kendall")
  # p-value = 0.7002
cor.test(auc.sal.noabx$AUC, auc.sal.noabx$BodyTemp, method = "kendall")
  # p-value = 0.3189





### Test Ketones Correlation ###
cor.test(auc.inu.abx$AUC, auc.inu.abx$Ketones, method = "kendall")
  # p-value = 0.2963
cor.test(auc.inu.noabx$AUC, auc.inu.noabx$Ketones, method = "kendall")
  # p-value = 0.524
cor.test(auc.sal.abx$AUC, auc.sal.abx$Ketones, method = "kendall")
  # p-value = 0.7837
cor.test(auc.sal.noabx$AUC, auc.sal.noabx$Ketones, method = "kendall")
  # p-value = 0.08113
```


### Load/Test Inulinase RPKG
```{r}
### Exo - RPKG
exo_rpkg <- read.csv('eCAMI/inulinase/exoRPKG.csv')
exo_rpkg$Season <- ordered(exo_rpkg$Season, c("Summer", "Winter", "Spring"))

# Remove W20 outlier
exo_rpkg <- exo_rpkg[-which(exo_rpkg$Sample == "S-W20"),]


### Prepare AUC & slope data
for(sample in 1:nrow(exo_rpkg)){
  sampName <- substr(exo_rpkg$Sample[sample], 3, 5)
  useRow <- which(auc.df$ID == sampName)
  exo_rpkg$AUC[sample] <- auc.df$AUC[useRow]
  useRowD <- which(d.metrics$ID == sampName)
  exo_rpkg$Slope[sample] <- d.metrics$Slope[useRowD]
  exo_rpkg$SlopeTime[sample] <- d.metrics$SlopeTime[useRowD]
  exo_rpkg$MaxD[sample] <- d.metrics$MaxD[useRowD]
  exo_rpkg$TimeMaxD[sample] <- d.metrics$TimeMaxD[useRowD]
}

# Subset out inulin samples
exo_rpkg_inu <- exo_rpkg[1:9,]


### Test RPKG - AUC
cor.test(exo_rpkg_inu$RPKG, exo_rpkg_inu$AUC, method = "kendall")
  # p-value = 0.9195

### Test RPKG - Slope
cor.test(exo_rpkg_inu$RPKG, exo_rpkg_inu$Slope, method = "kendall")
  # p-value = 0.9195

### Test RPKG - SlopeTime
cor.test(exo_rpkg_inu$RPKG, exo_rpkg_inu$SlopeTime, method = "kendall")
  # p-value = 0.3585

### Test RPKG - MaxD
cor.test(exo_rpkg_inu$RPKG, exo_rpkg_inu$MaxD, method = "kendall")
  # p-value = 0.9195

### Test RPKG - TimeMaxD
cor.test(exo_rpkg_inu$RPKG, exo_rpkg_inu$TimeMaxD, method = "kendall")
  # p-value = 0.1802
```


### Load/Test DNA Yield
```{r}
# Load DNA data
dna <- read.csv('dna.csv')
dna$Season <- ordered(dna$Season, levels=c("Summer", "Winter", "Spring"))
dna <- dna[-which(dna$Squirrel == "S37"),]
#dna <- dna[-which(dna$Squirrel == "S28"),]
dna <- dna[-which(dna$Squirrel == "W43"),]
dna <- dna[-which(dna$Squirrel == "W50"),]
dna <- dna[-which(dna$Squirrel == "P04"),]
dna.con <- dna[which(substr(dna$Sample,2,2) == "C"),]


### Prepare AUC & slope data
dna.con <- dna.con[-which(dna.con$Squirrel == "P28"),]
dna.con <- dna.con[-which(dna.con$Squirrel == "S11"),]
dna.con <- dna.con[-which(dna.con$Squirrel == "S40"),]
for(sample in 1:nrow(dna.con)){
  sampName <- dna.con$Squirrel[sample]
  useRow <- which(auc.df$ID == sampName)
  dna.con$AUC[sample] <- auc.df$AUC[useRow]
  useRowD <- which(d.metrics$ID == sampName)
  dna.con$Slope[sample] <- d.metrics$Slope[useRowD]
  dna.con$SlopeTime[sample] <- d.metrics$SlopeTime[useRowD]
  dna.con$MaxD[sample] <- d.metrics$MaxD[useRowD]
  dna.con$TimeMaxD[sample] <- d.metrics$TimeMaxD[useRowD]
}

# Subset out inulin
dna.c.inu <- dna.con[which(dna.con$Substrate == "Inulin"),]

# Subset out ABX treatments
dna.c.inu.abx <- dna.c.inu[which(dna.c.inu$Antibiotics == "Antibiotics"),]
dna.c.inu.noabx <- dna.c.inu[which(dna.c.inu$Antibiotics == "No Antibiotics"),]




cor.test(dna.c.inu.abx$AUC, dna.c.inu.abx$Slope, method = "kendall")
  # p-value = 0.4893
cor.test(dna.c.inu.noabx$AUC, dna.c.inu.noabx$Slope, method = "kendall")
  # p-value = 0.0001152**
  # tau = 0.5809524
cor.test(dna.c.sal.abx$AUC, dna.c.sal.abx$Slope, method = "kendall")
  # p-value = 0.4101
cor.test(dna.c.sal.noabx$AUC, dna.c.sal.noabx$Slope, method = "kendall")
  # p-value = 0.1126






### Test
cor.test(dna.c.inu.noabx$DNA_Yield_.ng.mg., dna.c.inu.noabx$AUC, method = "kendall")
  # p-value = 0.5306
cor.test(dna.c.inu.noabx$DNA_Yield_.ng.mg., dna.c.inu.noabx$Slope, method = "kendall")
  # p- value = 0.8815
cor.test(dna.c.inu.noabx$DNA_Yield_.ng.mg., dna.c.inu.noabx$SlopeTime, method = "kendall")
  # p-value = 0.4501
cor.test(dna.c.inu.noabx$DNA_Yield_.ng.mg., dna.c.inu.noabx$MaxD, method = "kendall")
  # p-value = 0.6982
cor.test(dna.c.inu.noabx$DNA_Yield_.ng.mg., dna.c.inu.noabx$TimeMaxD, method = "kendall")
  # p-value = 0.8559


cor.test(dna.c.inu.abx$DNA_Yield_.ng.mg., dna.c.inu.abx$AUC, method = "kendall")
  # p-value = 0.6739
cor.test(dna.c.inu.abx$DNA_Yield_.ng.mg., dna.c.inu.abx$Slope, method = "kendall")
  # p-value = 0.04958*
  # tau = 0.3303964
cor.test(dna.c.inu.abx$DNA_Yield_.ng.mg., dna.c.inu.abx$SlopeTime, method = "kendall")
  # p-value = 0.4826
cor.test(dna.c.inu.abx$DNA_Yield_.ng.mg., dna.c.inu.abx$MaxD, method = "kendall")
  # p-value = 0.2068
cor.test(dna.c.inu.abx$DNA_Yield_.ng.mg., dna.c.inu.abx$TimeMaxD, method = "kendall")
  # p-value = 0.1063
```


### Hib Dates
```{r}
### Load data ###

# Dates
dates <- read.csv('metadata_dates.csv')
dates_use <- dates[-which(dates$Season == "Summer"),]
rownames(dates_use) <- 1:nrow(dates_use)
dates_use <- dates_use[-13:-28,]
rownames(dates_use) <- 1:nrow(dates_use)
dates_use <- dates_use[-33:-40,]
dates_use <- dates_use[-3,]
  # Remove outlier P04

# Add d metrics + AUC
for(k in 1:nrow(dates_use)){
  ID <- dates_use$ID[k]
  
  rowUse <- which(d.metrics$ID == ID)
  dates_use$Slope[k] <- d.metrics$Slope[rowUse]
  
  rowUse2 <- which(auc.df$ID == ID)
  dates_use$AUC[k] <- auc.df$AUC[rowUse2]
  
  rowUse3 <- which(dna.con$Squirrel == ID)
  dates_use$DNA_Yield[k] <- dna.con$DNA_Yield_.ng.mg.[rowUse3]
  dates_use$Slope[k] <- dna.con$Slope[rowUse3]
  dates_use$SlopeTime[k] <- dna.con$SlopeTime[rowUse3]
  dates_use$MaxD[k] <- dna.con$MaxD[rowUse3]
  dates_use$TimeMaxD[k] <- dna.con$TimeMaxD[rowUse3]
}
  
# Separate by Substrate
dates.inu <- dates_use[which(dates_use$Substrate == "Inulin"),]
dates.sal <- dates_use[which(dates_use$Substrate == "Saline"),]

# Separate by Substrate + Season
dates.inu.p <- dates.inu[which(dates.inu$Season == "Spring"),]
dates.inu.w <- dates.inu[which(dates.inu$Season == "Winter"),]
dates.sal.p <- dates.sal[which(dates.sal$Season == "Spring"),]
dates.sal.w <- dates.sal[which(dates.sal$Season == "Winter"),]



### Correlate Hib Dates ###

# Inulin Spring - Slope
cor.test(dates.inu.p$Days_Hib, dates.inu.p$Slope, method = "kendall")
  # p-value = 0.8167
cor.test(dates.inu.p$IBA.Hib, dates.inu.p$Slope, method = "kendall")
  # p-value = 0.4833
cor.test(dates.inu.p$Num_IBA, dates.inu.p$Slope, method = "kendall")
  # p-value = 0.8167

# Inulin Spring - AUC
cor.test(dates.inu.p$Days_Hib, dates.inu.p$AUC, method = "kendall")
  # p-value = 0.4833
cor.test(dates.inu.p$IBA.Hib, dates.inu.p$AUC, method = "kendall")
  # p-value = 0.8167
cor.test(dates.inu.p$Num_IBA, dates.inu.p$AUC, method = "kendall")
  # p-value = 0.4833

# Inulin Spring - DNA_Yield
cor.test(dates.inu.p$Days_Hib, dates.inu.p$DNA_Yield, method = "kendall")
  # p-value = 0.2333
cor.test(dates.inu.p$IBA.Hib, dates.inu.p$DNA_Yield, method = "kendall")
  # p-value = 0.4833
cor.test(dates.inu.p$Num_IBA, dates.inu.p$DNA_Yield, method = "kendall")
  # p-value = 0.2333

# Inulin Spring - SlopeTime
cor.test(dates.inu.p$Days_Hib, dates.inu.p$SlopeTime, method = "kendall")
  # p-value = 1
cor.test(dates.inu.p$IBA.Hib, dates.inu.p$SlopeTime, method = "kendall")
  # p-value = 0.8167
cor.test(dates.inu.p$Num_IBA, dates.inu.p$SlopeTime, method = "kendall")
  # p-value = 1

# Inulin Spring - MaxD
cor.test(dates.inu.p$Days_Hib, dates.inu.p$MaxD, method = "kendall")
  # p-value = 0.8167
cor.test(dates.inu.p$IBA.Hib, dates.inu.p$MaxD, method = "kendall")
  # p-value = 1
cor.test(dates.inu.p$Num_IBA, dates.inu.p$MaxD, method = "kendall")
  # p-value = 0.8167

# Inulin Spring - TimeMaxD
cor.test(dates.inu.p$Days_Hib, dates.inu.p$TimeMaxD, method = "kendall")
  # p-value = 0.4833
cor.test(dates.inu.p$IBA.Hib, dates.inu.p$TimeMaxD, method = "kendall")
  # p-value = 0.8167
cor.test(dates.inu.p$Num_IBA, dates.inu.p$TimeMaxD, method = "kendall")
  # p-value = 1



# Inulin Winter - Slope
cor.test(dates.inu.w$Days_Hib, dates.inu.w$Slope, method = "kendall")
  # p-value = 0.3692
cor.test(dates.inu.w$IBA.Hib, dates.inu.w$Slope, method = "kendall")
  # p-value = 1
cor.test(dates.inu.w$Num_IBA, dates.inu.w$Slope, method = "kendall")
  # p-value = 0.4689

# Inulin Winter - AUC
cor.test(dates.inu.w$Days_Hib, dates.inu.w$AUC, method = "kendall")
  # p-value = 0.7194
cor.test(dates.inu.w$IBA.Hib, dates.inu.w$AUC, method = "kendall")
  # p-value = 0.6007
cor.test(dates.inu.w$Num_IBA, dates.inu.w$AUC, method = "kendall")
  # p-value = 0.7172

# Inulin Winter - DNA_Yield
cor.test(dates.inu.w$Days_Hib, dates.inu.w$DNA_Yield, method = "kendall")
  # p-value = 0.03114
  # adj p = 0.12456
    p.adjust(c(0.2333, 0.03114, 0.1361, 0.2773), "BH")
        # For all 4 comparisons of Days_Hib X DNA_Yield (Inulin & Saline, Spring & Winter)
        # 0.27730 0.12456 0.27220 0.27730
cor.test(dates.inu.w$IBA.Hib, dates.inu.w$DNA_Yield, method = "kendall")
  # p-value = 0.4843
cor.test(dates.inu.w$Num_IBA, dates.inu.w$DNA_Yield, method = "kendall")
  # p-value = 0.4689

# Inulin Winter - SlopeTime
cor.test(dates.inu.w$Days_Hib, dates.inu.w$SlopeTime, method = "kendall")
  # p-value = 0.01192*
  # tau = 0.6292532
cor.test(dates.inu.w$IBA.Hib, dates.inu.w$SlopeTime, method = "kendall")
  # p-value = 0.8618
cor.test(dates.inu.w$Num_IBA, dates.inu.w$SlopeTime, method = "kendall")
  # p-value = 0.205

# Inulin Winter - MaxD
cor.test(dates.inu.w$Days_Hib, dates.inu.w$MaxD, method = "kendall")
  # p-value = 0.3692
cor.test(dates.inu.w$IBA.Hib, dates.inu.w$MaxD, method = "kendall")
  # p-value = 1
cor.test(dates.inu.w$Num_IBA, dates.inu.w$MaxD, method = "kendall")
  # p-value = 0.4689

# Inulin Winter - TimeMaxD
cor.test(dates.inu.w$Days_Hib, dates.inu.w$TimeMaxD, method = "kendall")
  # p-value = 0.07249
cor.test(dates.inu.w$IBA.Hib, dates.inu.w$TimeMaxD, method = "kendall")
  # p-value = 0.3807
cor.test(dates.inu.w$Num_IBA, dates.inu.w$TimeMaxD, method = "kendall")
  # p-value = 0.8563






# Saline Spring - Slope
cor.test(dates.sal.p$Days_Hib, dates.sal.p$Slope, method = "kendall")
  # p-value = 0.4694
cor.test(dates.sal.p$IBA.Hib, dates.sal.p$Slope, method = "kendall")
  # p-value = 0.7194
cor.test(dates.sal.p$Num_IBA, dates.sal.p$Slope, method = "kendall")
  # p-value = 1

# Saline Spring - AUC
cor.test(dates.sal.p$Days_Hib, dates.sal.p$AUC, method = "kendall")
  # p-value = 0.4694
cor.test(dates.sal.p$IBA.Hib, dates.sal.p$AUC, method = "kendall")
  # p-value = 0.05556
cor.test(dates.sal.p$Num_IBA, dates.sal.p$AUC, method = "kendall")
  # p-value = 0.2511

# Saline Spring - DNA_Yield
cor.test(dates.sal.p$Days_Hib, dates.sal.p$DNA_Yield, method = "kendall")
  # p-value = 0.1361
cor.test(dates.sal.p$IBA.Hib, dates.sal.p$DNA_Yield, method = "kendall")
  # p-value = 0.7194 
cor.test(dates.sal.p$Num_IBA, dates.sal.p$DNA_Yield, method = "kendall")
  # p-value = 0.4442

# Saline Spring - SlopeTime
cor.test(dates.sal.p$Days_Hib, dates.sal.p$SlopeTime, method = "kendall")
  # p-value = 1
cor.test(dates.sal.p$IBA.Hib, dates.sal.p$SlopeTime, method = "kendall")
  # p-value = 0.7194
cor.test(dates.sal.p$Num_IBA, dates.sal.p$SlopeTime, method = "kendall")
  # p-value = 0.2511

# Saline Spring - MaxD
cor.test(dates.sal.p$Days_Hib, dates.sal.p$MaxD, method = "kendall")
  # p-value = 0.1072
cor.test(dates.sal.p$IBA.Hib, dates.sal.p$MaxD, method = "kendall")
  # p-value = 0.04407
cor.test(dates.sal.p$Num_IBA, dates.sal.p$MaxD, method = "kendall")
  # p-value = 0.4129

# Saline Spring - TimeMaxD
cor.test(dates.sal.p$Days_Hib, dates.sal.p$TimeMaxD, method = "kendall")
  # p-value = 1
cor.test(dates.sal.p$IBA.Hib, dates.sal.p$TimeMaxD, method = "kendall")
  # p-value = 1
cor.test(dates.sal.p$Num_IBA, dates.sal.p$TimeMaxD, method = "kendall")
  # p-value = 0.7021




# Saline Winter - Slope
cor.test(dates.sal.w$Days_Hib, dates.sal.w$Slope, method = "kendall")
  # p-value = 0.8563
cor.test(dates.sal.w$IBA.Hib, dates.sal.w$Slope, method = "kendall")
  # p-value = 0.8618
cor.test(dates.sal.w$Num_IBA, dates.sal.w$Slope, method = "kendall")
  # p-value = 0.8563

# Saline Winter - AUC
cor.test(dates.sal.w$Days_Hib, dates.sal.w$AUC, method = "kendall")
  # p-value = 0.4689
cor.test(dates.sal.w$IBA.Hib, dates.sal.w$AUC, method = "kendall")
  # p-value = 1
cor.test(dates.sal.w$Num_IBA, dates.sal.w$AUC, method = "kendall")
  # p-value = 1

# Saline Winter - DNA_Yield
cor.test(dates.sal.w$Days_Hib, dates.sal.w$DNA_Yield, method = "kendall")
  # p-value = 0.2773
cor.test(dates.sal.w$IBA.Hib, dates.sal.w$DNA_Yield, method = "kendall")
  # p-value = 0.8618
cor.test(dates.sal.w$Num_IBA, dates.sal.w$DNA_Yield, method = "kendall")
  # p-value = 0.587

# Saline Winter - SlopeTime
cor.test(dates.sal.w$Days_Hib, dates.sal.w$SlopeTime, method = "kendall")
  # p-value = 0.7851
cor.test(dates.sal.w$IBA.Hib, dates.sal.w$SlopeTime, method = "kendall")
  # p-value = 0.2812
cor.test(dates.sal.w$Num_IBA, dates.sal.w$SlopeTime, method = "kendall")
  # p-value = 0.3174

# Saline Winter - MaxD
cor.test(dates.sal.w$Days_Hib, dates.sal.w$MaxD, method = "kendall")
  # p-value = 0.9276
cor.test(dates.sal.w$IBA.Hib, dates.sal.w$MaxD, method = "kendall")
  # p-value = 0.8575
cor.test(dates.sal.w$Num_IBA, dates.sal.w$MaxD, method = "kendall")
  # p-value = 0.7851

# Saline Winter - TimeMaxD
cor.test(dates.sal.w$Days_Hib, dates.sal.w$TimeMaxD, method = "kendall")
  # p-value = 0.05632
cor.test(dates.sal.w$IBA.Hib, dates.sal.w$TimeMaxD, method = "kendall")
  # p-value = 0.3692
cor.test(dates.sal.w$Num_IBA, dates.sal.w$TimeMaxD, method = "kendall")
  # p-value = 0.7851





### Combine RPKG + Hib Dates
rpkg.dates <- exo_rpkg[which(exo_rpkg$Season != "Summer"),]

for(m in 1:nrow(rpkg.dates)){
  ID <- substr(rpkg.dates$Sample, 3, 5)[m]
  
  useRow <- which(dates_use$ID == ID)
  rpkg.dates$Days_Hib[m] <- dates_use$Days_Hib[useRow]
  rpkg.dates$IBA.Hib[m] <- dates_use$IBA.Hib[useRow]
  rpkg.dates$Num_IBA[m] <- dates_use$Num_IBA[useRow]
}

# Separate by Season
rpkg.dates.w <- rpkg.dates[which(rpkg.dates$Season == "Winter"),]
rpkg.dates.p <- rpkg.dates[which(rpkg.dates$Season == "Spring"),]



### Correlate RPKG w/ Hib Dates ###
cor.test(rpkg.dates.w$Days_Hib, rpkg.dates.w$RPKG, method = "kendall")
  # p-value = 0.6015
cor.test(rpkg.dates.p$Days_Hib, rpkg.dates.p$RPKG, method = "kendall")
  # p-value = 0.4694

cor.test(rpkg.dates.w$IBA.Hib, rpkg.dates.w$RPKG, method = "kendall")
  # p-value = 1
cor.test(rpkg.dates.p$IBA.Hib, rpkg.dates.p$RPKG, method = "kendall")
  # p-value = 0.7194

cor.test(rpkg.dates.w$Num_IBA, rpkg.dates.w$RPKG, method = "kendall")
  # p-value = 0.8005
cor.test(rpkg.dates.p$Num_IBA, rpkg.dates.p$RPKG, method = "kendall")
  # p-value = 0.7194
```